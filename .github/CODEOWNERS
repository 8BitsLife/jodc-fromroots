# Owner review is paused for now. To turn it back on, uncomment the line below
# and set required approvals to 1 with "Require review from Code Owners" in the
# `main` ruleset (.github/rulesets/main.json).
# * @8BitsLife
