node_modules
dist
.DS_Store
*.local
.env
.env.*
!.env.example

# TypeScript incremental build info
*.tsbuildinfo

# Local Claude Code / editor config
.claude/

# Vercel
.vercel

# Environment variables (keep keys out of git; set them in Vercel instead)
.env
.env.*
!.env.example
